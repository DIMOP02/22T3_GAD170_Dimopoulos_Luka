using System.Collections;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using UnityEngine;

namespace LukaDimopoulos
{
    public class Week2Activities : MonoBehaviour
    {
        private string favouriteGame = "Borderlands 2";
        private int hoursPlayed = 1672;
        private float costOfGame = 25f;

        // Start is called before the first frame update
        void Start()
        {
            Debug.Log("My favourite game is " + favouriteGame + "!");
        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
